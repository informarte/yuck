package yuck

/**
 * In comments, the following notation is used:
 *
 *  - Upper-case letters denote sets.
 *  - Lower-case variables denote set members and functions.
 *  - Typically, A and B denote value sets; I, J, and K denote index sets; and X and Y
 *    denote variable sets.
 *  - Typically, a and b denote values; i, j, and k denote indices; and x and y denotes
 *    variables.
 *  - s often denotes the current search state; it maps each variable to a value.
 *  - s' often denotes the next search state that results from changing variable assignments.
 */
object Notation
